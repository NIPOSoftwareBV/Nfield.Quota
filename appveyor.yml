branches:
  only:
    - master
    - development
    - /ci-.+/

version: 1.0.{build}

install:
  - ps: ./prerelease.ps1

assembly_info:
  patch: true
  file: '**\AssemblyInfo.*'
  assembly_version: '{version}'
  assembly_file_version: '{version}'
  assembly_informational_version: '$(assembly_informational_version)'

before_build:
  - nuget restore

build:
  publish_nuget: true
  publish_nuget_symbols: true
  verbosity: minimal

nuget:
  account_feed: true

deploy:
  # Create a GitHub release off the master branch without a tag
  - provider: Environment
    name: GitHub
    artifact: /.*\.nupkg/
    on:
      branch: master
      appveyor_repo_tag: false
  # Deploying to NuGet feed for Master with tag (release)
  - provider: NuGet
    api_key:
      secure: At04F3T5ap6Hl8NE26K/kUNVL2Vdk2HoKFk1yf5VmClq+NZ5RKF2r1owfohdgAf4
    skip_symbols: false
    artifact: /.*\.nupkg/
    on:
      branch: master
      appveyor_repo_tag: false
  # Deploying to NuGet feed for development as prerelease (determined by package name)
  - provider: NuGet
    api_key:
      secure: At04F3T5ap6Hl8NE26K/kUNVL2Vdk2HoKFk1yf5VmClq+NZ5RKF2r1owfohdgAf4
    skip_symbols: false
    artifact: /.*\.nupkg/
    on:
      branch: development
